export const API_URL = process.env.EXPO_PUBLIC_API_URL;

export const API_ENDPOINTS = {
  //register and auth endpoints
  api: '/api',
  auth: '/api/auth',
  session: '/api/auth/session',
  sessionInfo: '/api/auth/session/info',
  sessionLogout: '/api/auth/session/logout',
}